# Umami Cloud website ID (UUID). When unset, no analytics script is injected.
# Grab this from https://cloud.umami.is after adding thinkplain.ai as a website.
# Exposed client-side (required): the PUBLIC_ prefix is a SvelteKit contract.
PUBLIC_UMAMI_WEBSITE_ID=
