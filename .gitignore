# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code
.claude/settings.local.json
.claude/worktrees/

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
.temp/

# T23: raw LLM responses captured on premises rubric parse failures —
# debugging exhaust, not a build artifact (unlike the committed
# content/pipeline/<book>/*.json pipeline intermediates).
content/pipeline/social/parse-failures/

# Test output
test-results/

# iOS/Xcode
xcuserdata/
*.xcuserstate
*.xcuserdatad
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
__pycache__

# Node
node_modules/

.vercel

# Social pilot render output
social/out/

# Firebase Functions build output and CLI local state
functions/lib/
.firebase/
firebase-debug.log
firebase-debug.*.log

# Local social-pilot credentials — the file-backed TokenStore
# (social/src/publish/token-store-local.ts, --token-store local) holds live
# Instagram/YouTube tokens in plaintext. NEVER commit it.
content/social/tokens.local.json
content/social/tokens.*.json
